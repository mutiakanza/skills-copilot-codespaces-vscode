# Database
DATABASE_URL="postgresql://univuser:univpass@localhost:5432/univlearning?schema=public"

# Keycloak SSO
KEYCLOAK_URL="http://localhost:8080"
KEYCLOAK_REALM="university"
KEYCLOAK_CLIENT_ID="univ-learning-app"
KEYCLOAK_CLIENT_SECRET="your-client-secret"

# JWT
JWT_SECRET="your-jwt-secret-key-change-in-production"
JWT_EXPIRATION="30m"

# MinIO / S3
MINIO_ENDPOINT="localhost"
MINIO_PORT=9000
MINIO_ACCESS_KEY="minioadmin"
MINIO_SECRET_KEY="minioadmin"
MINIO_BUCKET="univ-learning"
MINIO_USE_SSL=false

# App Config
NODE_ENV="development"
PORT=3001
CORS_ORIGIN="http://localhost:3000"

# Email (Optional)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER=""
SMTP_PASSWORD=""
EMAIL_FROM="noreply@university.ac.id"
